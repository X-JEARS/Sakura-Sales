(() => {
  const DEMO = {
    user: { id: 'u1', username: 'admin', display_name: '现场管理员', role: 'admin', locale: 'zh-CN', theme: 'light' },
    events: [{ id: 'e1', slug: 'summer-market-7f3a91c2', name: '夏日同人展', currency_unit: 'CNY', start_at: '2026-08-28T09:00:00+08:00', end_at: '2026-08-30T18:00:00+08:00', manual_status: 'open' }, { id: 'e2', slug: 'autumn-pop-up', name: '秋季快闪贩售', currency_unit: 'CNY', start_at: '2026-10-01T10:00:00+08:00', end_at: '2026-10-03T18:00:00+08:00', manual_status: 'scheduled' }],
    products: [{ id: 'p1', event_id: 'e1', name: '限定徽章套装', price_minor: 1800 }, { id: 'p2', event_id: 'e1', name: '插画明信片', price_minor: 800 }, { id: 'p3', event_id: 'e1', name: '角色亚克力立牌', price_minor: 3200 }, { id: 'p4', event_id: 'e1', name: '帆布袋', price_minor: 4500 }, { id: 'p5', event_id: 'e1', name: '贴纸包', price_minor: 600 }],
    gifts: [{ id: 'g1', event_id: 'e1', threshold_minor: 5000, gift_name: '限定票根', mode: 'highest' }, { id: 'g2', event_id: 'e1', threshold_minor: 10000, gift_name: '特典海报', mode: 'cumulative' }], orders: []
  };
  const EXTRA = {
    'zh-CN': { account:'账号', accounts:'账号管理', displayName:'显示名称', role:'角色', create:'创建', update:'保存修改', eventName:'活动名称', currencyUnit:'价格单位', startAt:'开售时间', endAt:'停售时间', state:'销售状态', passwordHint:'初始密码', newPassword:'新密码', passwordOptional:'留空表示不修改密码', backToSale:'返回贩售', operator:'操作员', adminRole:'管理员', activityManage:'活动管理', editEvent:'编辑活动', editMember:'修改权限', editAccount:'编辑账号', members:'可访问人员', addMember:'添加人员', remove:'移除', noMembers:'暂无活动成员', giftName:'赠品名称', addGift:'添加规则', editProduct:'编辑商品', deleteProduct:'删除商品', deleteGift:'删除规则', deleteProductConfirm:'确定删除该商品？删除后不再显示在销售列表中。', deleteGiftConfirm:'确定删除该满赠规则？', uploadImage:'点击上传图片', removeImage:'移除图片', imageTooLarge:'图片不能超过 5MB', unsupportedImage:'仅支持 JPG / PNG / WebP / GIF', giftThreshold:'门槛', confirmDelete:'确认删除' },
    'zh-TW': { account:'帳號', accounts:'帳號管理', displayName:'顯示名稱', role:'角色', create:'建立', update:'儲存修改', eventName:'活動名稱', currencyUnit:'價格單位', startAt:'開售時間', endAt:'停售時間', state:'銷售狀態', passwordHint:'初始密碼', newPassword:'新密碼', passwordOptional:'留空表示不修改密碼', backToSale:'返回販售', operator:'操作員', adminRole:'管理員', activityManage:'活動管理', editEvent:'編輯活動', editMember:'修改權限', editAccount:'編輯帳號', members:'可存取人員', addMember:'新增人員', remove:'移除', noMembers:'暫無活動成員', giftName:'贈品名稱', addGift:'新增規則', editProduct:'編輯商品', deleteProduct:'刪除商品', deleteGift:'刪除規則', deleteProductConfirm:'確定刪除該商品？刪除後不再顯示在銷售清單中。', deleteGiftConfirm:'確定刪除該滿贈規則？', uploadImage:'點擊上傳圖片', removeImage:'移除圖片', imageTooLarge:'圖片不能超過 5MB', unsupportedImage:'僅支援 JPG / PNG / WebP / GIF', giftThreshold:'門檻', confirmDelete:'確認刪除' },
    en: { account:'Account', accounts:'Accounts', displayName:'Display name', role:'Role', create:'Create', update:'Save changes', eventName:'Event name', currencyUnit:'Price unit', startAt:'Sale starts', endAt:'Sale ends', state:'Sale status', passwordHint:'Initial password', newPassword:'New password', passwordOptional:'Leave blank to keep the current password', backToSale:'Back to sale', operator:'Operator', adminRole:'Administrator', activityManage:'Activity management', editEvent:'Edit event', editMember:'Edit permission', editAccount:'Edit account', members:'People with access', addMember:'Add person', remove:'Remove', noMembers:'No members yet', giftName:'Gift name', addGift:'Add rule', editProduct:'Edit product', deleteProduct:'Delete product', deleteGift:'Delete rule', deleteProductConfirm:'Delete this product? It will no longer appear in the sales list.', deleteGiftConfirm:'Delete this gift rule?', uploadImage:'Click to upload image', removeImage:'Remove image', imageTooLarge:'Image must be 5MB or smaller', unsupportedImage:'Only JPG / PNG / WebP / GIF supported', giftThreshold:'Threshold', confirmDelete:'Confirm delete' },
    ja: { account:'アカウント', accounts:'アカウント管理', displayName:'表示名', role:'役割', create:'作成', update:'変更を保存', eventName:'イベント名', currencyUnit:'価格単位', startAt:'販売開始', endAt:'販売終了', state:'販売状態', passwordHint:'初期パスワード', newPassword:'新しいパスワード', passwordOptional:'空欄の場合は変更しません', backToSale:'販売画面へ戻る', operator:'オペレーター', adminRole:'管理者', activityManage:'イベント管理', editEvent:'イベントを編集', editMember:'権限を変更', editAccount:'アカウントを編集', members:'アクセス可能なユーザー', addMember:'ユーザーを追加', remove:'削除', noMembers:'メンバーはいません', giftName:'贈品名', addGift:'ルール追加', editProduct:'商品を編集', deleteProduct:'商品を削除', deleteGift:'ルールを削除', deleteProductConfirm:'この商品を削除しますか？販売リストから表示されなくなります。', deleteGiftConfirm:'この特典ルールを削除しますか？', uploadImage:'クリックで画像をアップロード', removeImage:'画像を削除', imageTooLarge:'画像は5MB以下にしてください', unsupportedImage:'JPG / PNG / WebP / GIF のみ対応', giftThreshold:'しきい値', confirmDelete:'削除を確認' }
  };
  const T = {
    'zh-CN': { brand: '场贩订单台', subtitle: '现场销售与统计', login: '登录工作台', loginHint: '使用管理员创建的内部账号', username: '用户名', password: '密码', signIn: '登录', demo: '演示模式：admin / admin', events: '活动', eventList: '活动列表', orders: '订单', reports: '统计报表', settings: '个人设置', logout: '退出登录', welcome: '今天也顺利开售', chooseEvent: '选择一个活动开始录入订单', open: '销售中', scheduled: '待开始', closed: '已结束', draft: '草稿', enter: '进入', products: '销售商品', cart: '当前订单', total: '订单净额', sales: '销售金额', returns: '退货金额', gift: '满赠', noGift: '暂无满赠', placeOrder: '确认下单', confirmTitle: '确认提交订单？', confirmText: '订单提交后会立即计入活动统计。请确认商品数量和金额。', cancel: '返回修改', confirm: '确认下单', orderDone: '订单已完成', orderNo: '订单号', orderHistory: '订单记录', status: '状态', time: '时间', amount: '金额', action: '操作', cancelOrder: '取消订单', cancelled: '已取消', confirmed: '已确认', noOrders: '还没有订单记录', summary: '活动概览', productCount: '商品数', orderCount: '订单数', netSales: '净销售额', soldQty: '净销量', language: '界面语言', theme: '显示主题', light: '浅色', dark: '深色', system: '跟随系统', save: '保存设置', saved: '设置已保存', online: '在线', adminTools: '管理', addEvent: '新建活动', manage: '管理活动', product: '商品', quantity: '数量', giftRules: '满赠规则', threshold: '门槛', mode: '方式', highest: '最高档位', cumulative: '可累加', sample: '演示数据', close: '关闭', negativeHint: '负数数量用于退货' },
    'zh-TW': { brand: '場販訂單台', subtitle: '現場銷售與統計', login: '登入工作台', loginHint: '使用管理員建立的內部帳號', username: '使用者名稱', password: '密碼', signIn: '登入', demo: '示範模式：admin / admin', events: '活動', eventList: '活動列表', orders: '訂單', reports: '統計報表', settings: '個人設定', logout: '登出', welcome: '今天也順利開售', chooseEvent: '選擇一個活動開始輸入訂單', open: '銷售中', scheduled: '待開始', closed: '已結束', draft: '草稿', enter: '進入', products: '銷售商品', cart: '目前訂單', total: '訂單淨額', sales: '銷售金額', returns: '退貨金額', gift: '滿贈', noGift: '暫無滿贈', placeOrder: '確認下單', confirmTitle: '確認提交訂單？', confirmText: '訂單提交後會立即計入活動統計。請確認商品數量和金額。', cancel: '返回修改', confirm: '確認下單', orderDone: '訂單已完成', orderNo: '訂單號', orderHistory: '訂單記錄', status: '狀態', time: '時間', amount: '金額', action: '操作', cancelOrder: '取消訂單', cancelled: '已取消', confirmed: '已確認', noOrders: '還沒有訂單記錄', summary: '活動概覽', productCount: '商品數', orderCount: '訂單數', netSales: '淨銷售額', soldQty: '淨銷量', language: '介面語言', theme: '顯示主題', light: '淺色', dark: '深色', system: '跟隨系統', save: '儲存設定', saved: '設定已儲存', online: '在線', adminTools: '管理', addEvent: '新建活動', manage: '管理活動', product: '商品', quantity: '數量', giftRules: '滿贈規則', threshold: '門檻', mode: '方式', highest: '最高檔位', cumulative: '可累加', sample: '示範資料', close: '關閉', negativeHint: '負數數量用於退貨' },
    en: { brand: 'Field Orders', subtitle: 'On-site sales & stats', login: 'Sign in', loginHint: 'Use an account created by an administrator', username: 'Username', password: 'Password', signIn: 'Sign in', demo: 'Demo mode: admin / admin', events: 'Events', eventList: 'Event list', orders: 'Orders', reports: 'Reports', settings: 'Settings', logout: 'Sign out', welcome: 'Ready for today', chooseEvent: 'Choose an event to start taking orders', open: 'Open', scheduled: 'Scheduled', closed: 'Closed', draft: 'Draft', enter: 'Open event', products: 'Products', cart: 'Current order', total: 'Net total', sales: 'Sales', returns: 'Returns', gift: 'Gifts', noGift: 'No gifts yet', placeOrder: 'Place order', confirmTitle: 'Submit this order?', confirmText: 'This order will be included in the event statistics immediately.', cancel: 'Keep editing', confirm: 'Submit', orderDone: 'Order completed', orderNo: 'Order no.', orderHistory: 'Order history', status: 'Status', time: 'Time', amount: 'Amount', action: 'Action', cancelOrder: 'Cancel order', cancelled: 'Cancelled', confirmed: 'Confirmed', noOrders: 'No orders yet', summary: 'Event overview', productCount: 'Products', orderCount: 'Orders', netSales: 'Net sales', soldQty: 'Net units', language: 'Language', theme: 'Theme', light: 'Light', dark: 'Dark', system: 'System', save: 'Save settings', saved: 'Settings saved', online: 'Online', adminTools: 'Manage', addEvent: 'New event', manage: 'Manage event', product: 'Product', quantity: 'Qty', giftRules: 'Gift rules', threshold: 'Threshold', mode: 'Mode', highest: 'Highest tier', cumulative: 'Cumulative', sample: 'Demo data', close: 'Close', negativeHint: 'Negative quantity records returns' },
    ja: { brand: '現場注文台', subtitle: '販売と集計', login: 'ログイン', loginHint: '管理者が作成したアカウントを使用', username: 'ユーザー名', password: 'パスワード', signIn: 'ログイン', demo: 'デモ：admin / admin', events: 'イベント', eventList: 'イベント一覧', orders: '注文', reports: 'レポート', settings: '設定', logout: 'ログアウト', welcome: '本日も販売開始', chooseEvent: 'イベントを選択して注文を開始', open: '販売中', scheduled: '開始前', closed: '終了', draft: '下書き', enter: '入場', products: '商品', cart: '現在の注文', total: '純額', sales: '売上', returns: '返品', gift: '特典', noGift: '特典なし', placeOrder: '注文を確定', confirmTitle: '注文を送信しますか？', confirmText: '送信するとイベント集計に反映されます。', cancel: '編集に戻る', confirm: '送信', orderDone: '注文完了', orderNo: '注文番号', orderHistory: '注文履歴', status: '状態', time: '時間', amount: '金額', action: '操作', cancelOrder: '注文を取消', cancelled: '取消済み', confirmed: '確定済み', noOrders: '注文はありません', summary: '概要', productCount: '商品数', orderCount: '注文数', netSales: '純売上', soldQty: '純数量', language: '言語', theme: 'テーマ', light: 'ライト', dark: 'ダーク', system: 'システム', save: '保存', saved: '設定を保存しました', online: 'オンライン', adminTools: '管理', addEvent: '新規イベント', manage: '管理', product: '商品', quantity: '数量', giftRules: '特典ルール', threshold: 'しきい値', mode: '方式', highest: '最高ランク', cumulative: '累積', sample: 'デモデータ', close: '閉じる', negativeHint: 'マイナス数量は返品を記録します' }
  }; Object.keys(EXTRA).forEach(locale => Object.assign(T[locale], EXTRA[locale]));
  const state = { user: null, events: [], products: [], gifts: [], orders: [], users: [], members: [], screen: 'events', selectedEvent: null, cart: {}, modal: null, demo: false, sidebarCollapsed: localStorage.getItem('sidebarCollapsed') === '1', locale: localStorage.getItem('locale') || navigator.language || 'zh-CN', theme: localStorage.getItem('theme') || 'light' };
  state.locale = ['zh-CN','zh-TW','en','ja'].includes(state.locale) ? state.locale : 'zh-CN';
  const t = key => (T[state.locale] && T[state.locale][key]) || T['zh-CN'][key] || key;
  const esc = value => String(value ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const icon = (name, label = '') => {
    const paths = {
      x: '<path d="M6 6l12 12M18 6L6 18"/>',
      plus: '<path d="M12 5v14M5 12h14"/>',
      minus: '<path d="M5 12h14"/>',
      menu: '<path d="M4 7h16M4 12h16M4 17h16"/>',
      calendar: '<rect x="3" y="4.5" width="18" height="16" rx="2"/><path d="M16 3v3M8 3v3M3 9h18"/>',
      settings: '<path d="M12 15.5a3.5 3.5 0 1 0 0-7 3.5 3.5 0 0 0 0 7Z"/><path d="M19.4 15a1.7 1.7 0 0 0 .3 1.9l.1.1-2.1 2.1-.1-.1a1.7 1.7 0 0 0-1.9-.3 1.7 1.7 0 0 0-1 1.6v.2h-3v-.2a1.7 1.7 0 0 0-1-1.6 1.7 1.7 0 0 0-1.9.3l-.1.1-2.1-2.1.1-.1a1.7 1.7 0 0 0 .3-1.9 1.7 1.7 0 0 0-1.6-1h-.2v-3h.2a1.7 1.7 0 0 0 1.6-1 1.7 1.7 0 0 0-.3-1.9l-.1-.1 2.1-2.1.1.1a1.7 1.7 0 0 0 1.9.3 1.7 1.7 0 0 0 1-1.6v-.2h3v.2a1.7 1.7 0 0 0 1 1.6 1.7 1.7 0 0 0 1.9-.3l.1-.1 2.1 2.1-.1.1a1.7 1.7 0 0 0-.3 1.9 1.7 1.7 0 0 0 1.6 1h.2v3h-.2a1.7 1.7 0 0 0-1.6 1Z"/>',
      users: '<path d="M16 20v-1.5a3.5 3.5 0 0 0-3.5-3.5h-5A3.5 3.5 0 0 0 4 18.5V20"/><circle cx="10" cy="7" r="3"/><path d="M17 11a3 3 0 1 0-1.2-5.8M20 20v-1.5a3.5 3.5 0 0 0-2.3-3.3"/>',
      sliders: '<path d="M4 6h16M4 12h16M4 18h16"/><circle cx="9" cy="6" r="2"/><circle cx="15" cy="12" r="2"/><circle cx="10" cy="18" r="2"/>',
      sun: '<circle cx="12" cy="12" r="4"/><path d="M12 2v2M12 20v2M4.93 4.93l1.42 1.42M17.65 17.65l1.42 1.42M2 12h2M20 12h2M4.93 19.07l1.42-1.42M17.65 6.35l1.42-1.42"/>',
      moon: '<path d="M20.5 15.5A8.5 8.5 0 0 1 8.5 3.5 8.5 8.5 0 1 0 20.5 15.5Z"/>',
      chart: '<path d="M4 19V5M4 19h16"/><path d="m7 15 3-4 3 2 5-6"/>',
      edit: '<path d="m4 16.5-.8 3.3 3.3-.8L18.2 7.3a2.1 2.1 0 0 0-3-3L4 16.5Z"/><path d="m13.5 5.5 3 3"/>',
      list: '<path d="M8 6h12M8 12h12M8 18h12"/><path d="M4 6h.01M4 12h.01M4 18h.01"/>',
      package: '<path d="m4 7 8-4 8 4v10l-8 4-8-4Z"/><path d="m4 7 8 4 8-4M12 11v10"/>',
      image: '<rect x="3" y="4" width="18" height="16" rx="2"/><circle cx="8.5" cy="9" r="1.5"/><path d="m4 17 4.5-4.5 3 3 2.5-2.5L20 17"/>',
      arrowRight: '<path d="M5 12h14M13 6l6 6-6 6"/>',
      arrowLeft: '<path d="M19 12H5M11 18l-6-6 6-6"/>'
    };
    const title = label ? ` aria-label="${esc(label)}"` : ' aria-hidden="true"';
    return `<svg class="icon icon-${name}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"${title}>${paths[name] || paths.package}</svg>`;
  };
  const CURRENCIES = [
    { value: 'CNY', labels: { 'zh-CN':'人民币（CNY）', 'zh-TW':'人民幣（CNY）', en:'Chinese Yuan (CNY)', ja:'中国人民元（CNY）' } },
    { value: 'HKD', labels: { 'zh-CN':'港币（HKD）', 'zh-TW':'港幣（HKD）', en:'Hong Kong Dollar (HKD)', ja:'香港ドル（HKD）' } },
    { value: 'JPY', labels: { 'zh-CN':'日元（JPY）', 'zh-TW':'日圓（JPY）', en:'Japanese Yen (JPY)', ja:'日本円（JPY）' } },
    { value: 'USD', labels: { 'zh-CN':'美元（USD）', 'zh-TW':'美元（USD）', en:'US Dollar (USD)', ja:'米ドル（USD）' } }
  ];
  const LEGACY_CURRENCY_UNITS = { '元': 'CNY', '人民币': 'CNY', '港币': 'HKD', '日元': 'JPY', '美元': 'USD' };
  const currencyUnit = unit => { const normalized=LEGACY_CURRENCY_UNITS[unit] || unit; return CURRENCIES.some(c => c.value === normalized) ? normalized : 'CNY'; };
  const majorAmount = minor => (Number(minor || 0) / 100).toFixed(2);
  const minorAmount = major => Math.max(0, Math.round((Number.parseFloat(major) || 0) * 100));
  const moneyForUnit = (minor, unit) => `${majorAmount(minor)} ${currencyUnit(unit)}`;
  const money = minor => moneyForUnit(minor, state.selectedEvent?.currency_unit);
  const currencySelect = (selected, name = 'currency_unit') => `<select name="${name}">${CURRENCIES.map(c => `<option value="${c.value}" ${currencyUnit(selected)===c.value?'selected':''}>${c.labels[state.locale] || c.labels.en}</option>`).join('')}</select>`;
  const amountLabel = (label, unit) => `${label} (${currencyUnit(unit)})`;
  const fmtDate = value => value ? new Intl.DateTimeFormat(state.locale, { month:'short', day:'numeric', hour:'2-digit', minute:'2-digit' }).format(new Date(value)) : '-';
  const eventStatus = e => e.manual_status === 'open' ? 'open' : e.manual_status === 'closed' ? 'closed' : e.manual_status === 'scheduled' ? 'scheduled' : 'draft';
  const statusLabel = s => t(s === 'open' ? 'open' : s === 'scheduled' ? 'scheduled' : s === 'closed' ? 'closed' : 'draft');
  const api = async (path, options = {}) => { const res = await fetch(`/api${path}`, { credentials:'include', headers:{'Content-Type':'application/json', ...(options.headers || {})}, ...options }); if (!res.ok) throw new Error((await res.json().catch(() => ({}))).error || `HTTP ${res.status}`); return res.json(); };
  const uploadImage = async (file, eventId) => { try { const res = await fetch(`/api/events/${eventId}/images`, { method:'POST', credentials:'include', headers:{'Content-Type':file.type}, body:file }); if (!res.ok) throw new Error((await res.json().catch(() => ({}))).error || `HTTP ${res.status}`); return res.json(); } catch { return { url: await new Promise((resolve, reject) => { const reader = new FileReader(); reader.onload = () => resolve(reader.result); reader.onerror = reject; reader.readAsDataURL(file); }) }; } };
  const toast = message => { const el = document.querySelector('#toast'); el.textContent = message; el.classList.add('show'); setTimeout(() => el.classList.remove('show'), 2600); };
  const applyTheme = () => { const resolvedTheme = state.theme === 'system' ? (matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light') : state.theme; document.documentElement.dataset.theme = resolvedTheme; const themeColor = document.querySelector('meta[name="theme-color"]'); const background = getComputedStyle(document.documentElement).getPropertyValue('--bg').trim(); if (themeColor && background) themeColor.setAttribute('content', background); };
  const seedDemo = () => { state.demo = true; state.user = structuredClone(DEMO.user); state.events = JSON.parse(localStorage.getItem('demoEvents') || JSON.stringify(DEMO.events)); state.products = structuredClone(DEMO.products); state.gifts = structuredClone(DEMO.gifts); state.orders = JSON.parse(localStorage.getItem('demoOrders') || '[]'); state.users = [structuredClone(DEMO.user)]; };
  async function boot() { applyTheme(); try { const data = await api('/bootstrap'); state.demo=false; state.user=data.user; state.events=data.events; state.products=data.products; state.gifts=data.gifts; state.orders=data.orders || []; state.users=data.users || []; } catch { if (['localhost','127.0.0.1'].includes(location.hostname)) seedDemo(); else state.user = null; } checkRoute(); render(); }
  function checkRoute() { const m = location.pathname.match(/^\/events\/([^/]+)$/); if (m) { const ev = state.events.find(e => e.slug === m[1]); if (ev) { state.selectedEvent = ev; state.screen = 'event'; state.cart = {}; } } }
  function loginScreen() { return `<main class="login-page"><form class="login-card" id="loginForm"><div class="brand"><div class="brand-mark">台</div><div><strong>${t('brand')}</strong><small>${t('subtitle')}</small></div></div><h1>${t('login')}</h1><p>${t('loginHint')}</p><div class="field"><label>${t('username')}</label><input name="username" autocomplete="username" required value="admin"></div><div class="field"><label>${t('password')}</label><input name="password" type="password" autocomplete="current-password" required value="admin"></div><button class="btn btn-primary" style="width:100%">${t('signIn')} ${icon('arrowRight')}</button><div class="login-demo">${t('demo')}</div></form></main>`; }
  function shell() { const isAdmin = ['super_admin','admin','event_admin'].includes(state.user.role); const isSystem=['super_admin','admin'].includes(state.user.role); const collapsed = state.sidebarCollapsed ? ' sidebar-collapsed' : ''; return `<div class="app-shell${collapsed}"><aside class="sidebar" id="sidebar"><div class="brand"><div class="brand-mark">台</div><div><strong>${t('brand')}</strong><small>${t('subtitle')}</small></div></div><button class="sidebar-close" id="sidebarClose" aria-label="${t('close')}">${icon('x')}</button><nav class="nav"><button class="${state.screen==='events'?'active':''}" data-nav="events">${icon('calendar')}<span>${t('eventList')}</span></button>${isAdmin ? `<button class="${state.screen==='manage'?'active':''}" data-nav="manage">${icon('settings')}<span>${t('activityManage')}</span></button>` : ''}${isSystem ? `<button class="${state.screen==='accounts'?'active':''}" data-nav="accounts">${icon('users')}<span>${t('accounts')}</span></button>` : ''}<button class="${state.screen==='settings'?'active':''}" data-nav="settings">${icon('sliders')}<span>${t('settings')}</span></button></nav><div class="sidebar-footer">${state.demo ? t('sample') : 'Cloudflare D1'}<br>v0.2.0</div></aside><div class="sidebar-backdrop" id="sidebarBackdrop" aria-hidden="true"></div><section class="workspace"><header class="topbar"><div class="topbar-actions"><button class="icon-btn mobile-menu" id="menuBtn" aria-label="menu" aria-expanded="false">${icon('menu')}</button><h1>${state.screen==='event' ? esc(state.selectedEvent?.name) : state.screen==='event-edit' ? t('editEvent') : state.screen==='reports' ? t('reports') : state.screen==='orders' ? t('orderHistory') : state.screen==='settings' ? t('settings') : state.screen==='manage' ? t('activityManage') : state.screen==='accounts' ? t('accounts') : t('eventList')}</h1></div><div class="topbar-actions"><span class="status open"><span class="status-dot" aria-hidden="true"></span>${t('online')}</span><button class="icon-btn" id="themeBtn" title="${t('theme')}" aria-label="${t('theme')}">${state.theme==='dark'?icon('sun'):icon('moon')}</button><div class="user-pill"><div class="avatar">${esc((state.user.display_name || state.user.username).slice(0,1))}</div><span>${esc(state.user.display_name || state.user.username)}</span></div></div></header><main class="content">${renderScreen()}</main></section></div>`; }
  function renderScreen() { if (state.screen === 'event') return eventScreen(); if (state.screen === 'event-edit') return eventEditScreen(); if (state.screen === 'reports') return reportsScreen(); if (state.screen === 'orders') return ordersScreen(); if (state.screen === 'settings') return settingsScreen(); if (state.screen === 'manage') return manageScreen(); if (state.screen === 'accounts') return accountsScreen(); return eventsScreen(); }
  function eventsScreen() { const canEdit=['super_admin','admin','event_admin'].includes(state.user.role); const canReport=canEdit; return `<div class="page-head"><div><h2>${t('welcome')}</h2><p>${t('chooseEvent')}</p></div><button class="btn btn-primary" data-action="new-event">${icon('plus')}${t('addEvent')}</button></div><div class="event-grid">${state.events.map(e => `<article class="event-card"><div class="event-card-head"><h3>${esc(e.name)}</h3><span class="status ${eventStatus(e)}">${statusLabel(eventStatus(e))}</span></div><p class="event-date">${fmtDate(e.start_at)} — ${fmtDate(e.end_at)}</p><div class="event-card-footer"><small style="color:var(--muted)">/events/${esc(e.slug)}</small><div class="event-card-actions">${canReport?`<button class="btn btn-ghost icon-action" data-action="sales" data-id="${e.id}" title="${t('reports')}" aria-label="${t('reports')}">${icon('chart')}</button>`:''}${canEdit?`<button class="btn btn-ghost icon-action" data-action="edit-event" data-id="${e.id}" title="${t('editEvent')}" aria-label="${t('editEvent')}">${icon('edit')}</button>`:''}<button class="btn btn-primary" data-action="open-event" data-id="${e.id}">${t('enter')}</button></div></div></article>`).join('')}</div>`; }
  function calcCart() { const items = state.products.filter(p=>p.event_id===state.selectedEvent?.id).map(p => ({...p, quantity:Number(state.cart[p.id] || 0), line:Number(state.cart[p.id] || 0)*p.price_minor})).filter(p=>p.quantity); const net=items.reduce((s,p)=>s+p.line,0); const sales=items.filter(p=>p.quantity>0).reduce((s,p)=>s+p.line,0); const returns=items.filter(p=>p.quantity<0).reduce((s,p)=>s+p.line,0); const base=Math.max(0,net); const rules=state.gifts.filter(g=>g.event_id===state.selectedEvent?.id).filter(g=>g.threshold_minor<=base); const highest=Math.max(0,...rules.map(g=>g.threshold_minor)); const gifts=rules.filter(g=>g.mode==='cumulative' || g.threshold_minor===highest).map(g=>({...g,quantity:g.mode==='cumulative'?Math.floor(base/g.threshold_minor):1})); return {items,net,sales,returns,gifts}; }
  function checkoutBarHTML(){const c=calcCart();return `${c.gifts.length?`<div class="checkout-gifts">${c.gifts.map(g=>`<span class="gift-tag">${esc(g.gift_name)} ×${g.quantity}</span>`).join('')}</div>`:''}<div class="checkout-main"><div class="checkout-amount">${c.returns<0?`<div class="amount-detail"><small>${t('sales')} ${money(c.sales)}</small><small>${t('returns')} ${money(c.returns)}</small></div>`:''}<strong class="amount-total">${money(c.net)}</strong></div><button class="btn btn-primary" data-action="place-order" ${c.items.length?'':'disabled'}>${t('placeOrder')} ${icon('arrowRight')}</button></div>`;}
  function syncCheckout(){const bar=document.querySelector('.checkout-bar');if(!bar)return;bar.innerHTML=checkoutBarHTML();bar.querySelectorAll('[data-action]').forEach(b=>b.onclick=()=>actions(b.dataset.action,b.dataset));}
function eventScreen() { if (!state.selectedEvent) return eventsScreen(); const c=calcCart(); return `<div class="page-head event-page-head"><div><h2 class="event-title">${esc(state.selectedEvent.name)} <span class="status ${eventStatus(state.selectedEvent)}">${statusLabel(eventStatus(state.selectedEvent))}</span></h2></div><button class="btn btn-ghost" data-action="view-orders">${icon('list')}${t('orderHistory')}</button></div><div class="event-order"><div class="section-bar"><h2>${t('products')}</h2><span style="color:var(--muted);font-size:13px">${state.products.filter(p=>p.event_id===state.selectedEvent.id).length} ${t('product')}</span></div><div class="product-grid">${state.products.filter(p=>p.event_id===state.selectedEvent.id).map(p=>`<article class="product-card"><div class="product-card-main"><div class="product-img-square">${p.image_key?`<img src="${esc(p.image_key)}" alt="${esc(p.name)}">`:`${icon('package')}`}</div><div class="product-info"><div class="product-name">${esc(p.name)}</div><div class="product-price">${money(p.price_minor)}</div></div></div><div class="stepper"><button data-action="step" data-id="${p.id}" data-delta="-1" aria-label="Decrease quantity">${icon('minus')}</button><input class="qty ${(state.cart[p.id]||0)<0?'negative':''}" type="text" inputmode="numeric" pattern="[0-9-]*" data-id="${p.id}" value="${state.cart[p.id]||0}" aria-label="${t('quantity')}" autocomplete="off"><button data-action="step" data-id="${p.id}" data-delta="1" aria-label="Increase quantity">${icon('plus')}</button></div></article>`).join('')}</div></div><div class="checkout-bar">${checkoutBarHTML()}</div>`; }
  function eventEditScreen(){
    const e=state.selectedEvent;if(!e)return eventsScreen();
    const products=state.products.filter(p=>p.event_id===e.id);
    const gifts=state.gifts.filter(g=>g.event_id===e.id);
    return `<div class="page-head"><div><h2>${t('editEvent')}</h2><p>${esc(e.name)}</p></div><button class="btn btn-ghost" data-action="back-events">${icon('arrowLeft')} ${t('eventList')}</button></div>
      <section class="settings"><form id="eventEditForm"><div class="field"><label>${t('eventName')}</label><input name="name" value="${esc(e.name)}" required></div><div class="form-grid"><div class="field"><label>${t('currencyUnit')}</label>${currencySelect(e.currency_unit)}</div><div class="field"><label>${t('state')}</label><select name="manual_status"><option value="draft" ${e.manual_status==='draft'?'selected':''}>${t('draft')}</option><option value="open" ${e.manual_status==='open'?'selected':''}>${t('open')}</option><option value="scheduled" ${e.manual_status==='scheduled'?'selected':''}>${t('scheduled')}</option><option value="closed" ${e.manual_status==='closed'?'selected':''}>${t('closed')}</option></select></div><div class="field"><label>${t('startAt')}</label><input type="datetime-local" name="start_at" value="${e.start_at?new Date(e.start_at).toISOString().slice(0,16):''}"></div><div class="field"><label>${t('endAt')}</label><input type="datetime-local" name="end_at" value="${e.end_at?new Date(e.end_at).toISOString().slice(0,16):''}"></div></div><button class="btn btn-primary">${t('update')}</button></form></section>
      <section style="margin-top:28px"><div class="section-bar"><h2>${t('products')}</h2><button class="btn btn-primary" data-action="new-product">${icon('plus')}${t('product')}</button></div><div class="table-wrap"><table class="data-table"><thead><tr><th></th><th>${t('product')}</th><th>${t('amount')}</th><th>${t('action')}</th></tr></thead><tbody>${products.length?products.map(p=>`<tr><td><div class="thumb">${p.image_key?`<img src="${esc(p.image_key)}" alt="${esc(p.name)}">`:`${icon('package')}`}</div></td><td>${esc(p.name)}</td><td>${money(p.price_minor)}</td><td class="row-actions"><button class="btn btn-ghost icon-action" data-action="edit-product" data-id="${p.id}" aria-label="${t('editProduct')}" title="${t('editProduct')}">${icon('edit')}</button><button class="btn btn-danger icon-action" data-action="delete-product" data-id="${p.id}" data-name="${esc(p.name)}" aria-label="${t('deleteProduct')}" title="${t('deleteProduct')}">${icon('x')}</button></td></tr>`).join(''):`<tr><td colspan="4"><div class="empty">${t('product')}</div></td></tr>`}</tbody></table></div></section>
      <section style="margin-top:28px"><div class="section-bar"><h2>${t('giftRules')}</h2><button class="btn btn-primary" data-action="new-gift">${icon('plus')}${t('addGift')}</button></div>${gifts.length?`<div class="table-wrap"><table class="data-table"><thead><tr><th>${t('threshold')}</th><th>${t('giftName')}</th><th>${t('mode')}</th><th>${t('action')}</th></tr></thead><tbody>${gifts.map(g=>`<tr><td>${money(g.threshold_minor)}</td><td>${esc(g.gift_name)}</td><td>${t(g.mode)}</td><td class="row-actions"><button class="btn btn-ghost icon-action" data-action="edit-gift" data-id="${g.id}" aria-label="${t('editProduct')}" title="${t('editProduct')}">${icon('edit')}</button><button class="btn btn-danger icon-action" data-action="delete-gift" data-id="${g.id}" data-name="${esc(g.gift_name)}" aria-label="${t('deleteGift')}" title="${t('deleteGift')}">${icon('x')}</button></td></tr>`).join('')}</tbody></table></div>`:`<div class="empty">${t('noGift')}</div>`}</section>
      <section style="margin-top:28px"><div class="section-bar"><h2>${t('members')}</h2><button class="btn btn-primary" data-action="add-member">${icon('plus')}${t('addMember')}</button></div>${state.members.length?`<div class="table-wrap"><table class="data-table"><thead><tr><th>${t('displayName')}</th><th>${t('role')}</th><th>${t('action')}</th></tr></thead><tbody>${state.members.map(m=>`<tr><td>${esc(m.display_name)} <small style="color:var(--muted)">@${esc(m.username)}</small></td><td>${esc(m.event_role)}</td><td class="row-actions"><button class="btn btn-ghost icon-action" data-action="edit-member" data-id="${m.id}" aria-label="${t('editMember')}" title="${t('editMember')}">${icon('edit')}</button><button class="btn btn-danger icon-action" data-action="remove-member" data-id="${m.id}" aria-label="${t('remove')}" title="${t('remove')}">${icon('x')}</button></td></tr>`).join('')}</tbody></table></div>`:`<div class="empty">${t('noMembers')}</div>`}</section>`;
  }
  function accountsScreen(){return `<div class="page-head"><div><h2>${t('accounts')}</h2><p>${t('chooseEvent')}</p></div><button class="btn btn-primary" data-action="new-user">${icon('plus')}${t('account')}</button></div><div class="table-wrap"><table class="data-table"><thead><tr><th>${t('username')}</th><th>${t('displayName')}</th><th>${t('role')}</th><th>${t('status')}</th><th>${t('action')}</th></tr></thead><tbody>${state.users.map(u=>`<tr><td>${esc(u.username)}</td><td>${esc(u.display_name)}</td><td>${esc(u.role)}</td><td><span class="status ${u.status==='disabled'?'closed':'open'}">${esc(u.status||'active')}</span></td><td class="row-actions"><button class="btn btn-ghost icon-action" data-action="edit-user" data-id="${u.id}" aria-label="${t('editAccount')}" title="${t('editAccount')}">${icon('edit')}</button></td></tr>`).join('')}</tbody></table></div>`;}
  function ordersScreen() { const orders=state.orders.filter(o=>!state.selectedEvent || o.event_id===state.selectedEvent.id); return `<div class="page-head"><div><h2>${t('orderHistory')}</h2><p>${state.selectedEvent?esc(state.selectedEvent.name):t('chooseEvent')}</p></div>${state.selectedEvent?`<button class="btn btn-ghost" data-action="back-event">${icon('arrowLeft')} ${t('backToSale')}</button>`:''}</div>${orders.length?`<div class="table-wrap"><table class="data-table"><thead><tr><th>${t('orderNo')}</th><th>${t('time')}</th><th>${t('amount')}</th><th>${t('status')}</th><th>${t('action')}</th></tr></thead><tbody>${orders.map(o=>`<tr><td><strong>${esc(o.order_no)}</strong></td><td>${fmtDate(o.created_at)}</td><td>${money(o.net_amount_minor)}</td><td><span class="status ${o.status==='cancelled'?'closed':'open'}">${o.status==='cancelled'?t('cancelled'):t('confirmed')}</span></td><td>${o.status!=='cancelled'?`<button class="btn btn-danger" data-action="cancel-order" data-id="${o.id}">${t('cancelOrder')}</button>`:'-'}</td></tr>`).join('')}</tbody></table></div>`:`<div class="empty">${t('noOrders')}</div>`}`; }
  function reportsScreen(){const e=state.selectedEvent;if(!e)return eventsScreen();const products=state.products.filter(p=>p.event_id===e.id);const orders=state.orders.filter(o=>o.event_id===e.id&&o.status!=='cancelled');const net=orders.reduce((s,o)=>s+o.net_amount_minor,0);return `<div class="page-head"><div><h2>${t('reports')}</h2><p>${esc(e.name)}</p></div><button class="btn btn-ghost" data-action="back-events">${icon('arrowLeft')} ${t('eventList')}</button></div><div class="stats-grid"><div class="stat"><div class="stat-label">${t('netSales')}</div><div class="stat-value">${money(net)}</div></div><div class="stat"><div class="stat-label">${t('orderCount')}</div><div class="stat-value">${orders.length}</div></div><div class="stat"><div class="stat-label">${t('productCount')}</div><div class="stat-value">${products.length}</div></div><div class="stat"><div class="stat-label">${t('soldQty')}</div><div class="stat-value">${orders.reduce((s,o)=>s+(o.items||[]).reduce((x,i)=>x+i.quantity,0),0)}</div></div></div><div class="table-wrap"><table class="data-table"><thead><tr><th>${t('product')}</th><th>${t('amount')}</th><th>${t('quantity')}</th></tr></thead><tbody>${products.map(p=>{const q=orders.reduce((s,o)=>s+(o.items||[]).filter(i=>i.product_id===p.id).reduce((x,i)=>x+i.quantity,0),0);return `<tr><td>${esc(p.name)}</td><td>${money(p.price_minor*q)}</td><td>${q}</td></tr>`}).join('')}</tbody></table></div>`;}
  function manageScreen() { return `<div class="page-head"><div><h2>${t('activityManage')}</h2><p>${t('chooseEvent')}</p></div><button class="btn btn-primary" data-action="new-event">${icon('plus')}${t('addEvent')}</button></div><div class="event-grid">${state.events.map(e=>`<article class="event-card"><div class="event-card-head"><h3>${esc(e.name)}</h3><span class="status ${eventStatus(e)}">${statusLabel(eventStatus(e))}</span></div><p class="event-date">${fmtDate(e.start_at)} — ${fmtDate(e.end_at)}</p><div class="event-card-actions event-card-manage-actions"><button class="btn btn-primary" data-action="edit-event" data-id="${e.id}">${icon('edit')}${t('editEvent')}</button><button class="btn btn-ghost icon-action" data-action="sales" data-id="${e.id}" title="${t('reports')}" aria-label="${t('reports')}">${icon('chart')}</button></div></article>`).join('')}</div>`; }
  function settingsScreen() { return `<div class="page-head"><div><h2>${t('settings')}</h2><p>${esc(state.user.display_name || state.user.username)}</p></div></div><section class="settings"><div class="setting-row"><div><label>${t('username')}</label><small>${t('account')}</small></div><input id="usernameSetting" value="${esc(state.user.username)}" autocomplete="username"></div><div class="setting-row"><div><label>${t('displayName')}</label><small>${t('account')}</small></div><input id="displayNameSetting" value="${esc(state.user.display_name || '')}" autocomplete="name"></div><div class="setting-row"><div><label>${t('newPassword')}</label><small>${t('passwordOptional')}</small></div><input id="passwordSetting" type="password" autocomplete="new-password"></div><div class="setting-row"><div><label>${t('language')}</label><small>Language / 語言 / 言語</small></div><select id="localeSelect"><option value="zh-CN" ${state.locale==='zh-CN'?'selected':''}>简体中文</option><option value="zh-TW" ${state.locale==='zh-TW'?'selected':''}>繁體中文</option><option value="en" ${state.locale==='en'?'selected':''}>English</option><option value="ja" ${state.locale==='ja'?'selected':''}>日本語</option></select></div><div class="setting-row"><div><label>${t('theme')}</label><small>${t('light')} / ${t('dark')} / ${t('system')}</small></div><select id="themeSelect"><option value="light" ${state.theme==='light'?'selected':''}>${t('light')}</option><option value="dark" ${state.theme==='dark'?'selected':''}>${t('dark')}</option><option value="system" ${state.theme==='system'?'selected':''}>${t('system')}</option></select></div><button class="btn btn-primary" style="margin-top:20px" data-action="save-settings">${t('save')}</button><button class="btn btn-ghost" style="margin-top:20px;margin-left:8px" data-action="logout">${t('logout')}</button></section>`; }
  function render() { applyTheme(); document.title=t('brand'); document.querySelector('#app').innerHTML=state.user?shell():loginScreen(); bindEvents(); }
  function bindEvents() { const setSidebarOpen = open => { document.querySelector('#sidebar')?.classList.toggle('open', open); document.querySelector('#sidebarBackdrop')?.classList.toggle('open', open); document.body.classList.toggle('sidebar-open', open); const menu=document.querySelector('#menuBtn'); if(menu)menu.setAttribute('aria-expanded',String(open)); }; document.querySelectorAll('[data-nav]').forEach(b=>b.onclick=()=>{state.screen=b.dataset.nav; if(state.screen==='orders'&&!state.selectedEvent)state.selectedEvent=null; setSidebarOpen(false); render();}); document.querySelectorAll('[data-action]').forEach(b=>b.onclick=()=>actions(b.dataset.action,b.dataset)); document.querySelectorAll('input.qty').forEach(inp=>{inp.onfocus=function(){this.select();};inp.onclick=function(){this.select();};inp.oninput=function(){const m=String(this.value).match(/^-?\d+/);const val=m?parseInt(m[0],10):0;state.cart[this.dataset.id]=val;this.classList.toggle('negative',val<0);syncCheckout();};inp.onblur=function(){const m=String(this.value).match(/^-?\d+/);const val=m?parseInt(m[0],10):0;state.cart[this.dataset.id]=val;this.value=val;this.classList.toggle('negative',val<0);syncCheckout();};}); const form=document.querySelector('#loginForm'); if(form)form.onsubmit=login; const editForm=document.querySelector('#eventEditForm'); if(editForm)editForm.onsubmit=e=>{e.preventDefault();submitForm('event',state.selectedEvent.id,new FormData(editForm));state.screen='event-edit';}; const themeBtn=document.querySelector('#themeBtn'); if(themeBtn)themeBtn.onclick=()=>{state.theme=state.theme==='dark'?'light':'dark'; localStorage.setItem('theme',state.theme); render();}; const menu=document.querySelector('#menuBtn'); if(menu)menu.onclick=()=>{if(innerWidth<=640){const open=!document.querySelector('#sidebar')?.classList.contains('open'); setSidebarOpen(open); return;} state.sidebarCollapsed=!state.sidebarCollapsed; localStorage.setItem('sidebarCollapsed',state.sidebarCollapsed?'1':'0'); render();}; const closeBtn=document.querySelector('#sidebarClose'); if(closeBtn)closeBtn.onclick=()=>setSidebarOpen(false); const backdrop=document.querySelector('#sidebarBackdrop'); if(backdrop)backdrop.onclick=()=>setSidebarOpen(false); }
  async function login(event) { event.preventDefault(); const data=Object.fromEntries(new FormData(event.target)); try { const result=await api('/auth/login',{method:'POST',body:JSON.stringify(data)}); state.user=result.user; await boot(); } catch { if(data.username==='admin'&&data.password==='admin'){ seedDemo(); render(); } else toast('Invalid credentials'); } }
  async function actions(action, data) { if(action==='open-event'){state.selectedEvent=state.events.find(e=>e.id===data.id); state.screen='event'; state.cart={}; render();} if(action==='view-orders'){state.screen='orders';render();} if(action==='back-events'){state.screen='events';state.selectedEvent=null;render();} if(action==='back-event'){state.screen='event';render();} if(action==='step'){state.cart[data.id]=(state.cart[data.id]||0)+Number(data.delta);render();} if(action==='place-order'){state.modal={kind:'confirm'};renderModal();} if(action==='confirm-order'){await submitOrder();} if(action==='close-modal'){closeModal();} if(action==='cancel-order'){await cancelOrder(data.id);} if(action==='save-settings'){state.locale=document.querySelector('#localeSelect').value;state.theme=document.querySelector('#themeSelect').value;localStorage.setItem('locale',state.locale);localStorage.setItem('theme',state.theme);try{await api('/auth/preferences',{method:'PATCH',body:JSON.stringify({locale:state.locale,theme:state.theme})});}catch{} toast(t('saved'));render();} if(action==='logout'){try{await api('/auth/logout',{method:'POST'});}catch{}state.user=null;render();} if(action==='new-event'){openForm('event');} if(action==='edit-event'){state.selectedEvent=state.events.find(e=>e.id===data.id);state.screen='event-edit';try{state.members=(await api(`/events/${data.id}/members`)).results||[];}catch{state.members=[];}render();} if(action==='sales'){state.selectedEvent=state.events.find(e=>e.id===data.id);state.screen='reports';render();} if(action==='new-product'){openForm('product');} if(action==='new-user'){openForm('user');} if(action==='edit-product'){openForm('product',data.id);} if(action==='delete-product'){const name=data.name||state.products.find(p=>p.id===data.id)?.name||'';closeModal();state.modal={kind:'confirm-delete',payload:{type:'product',id:data.id,name}};renderModal();} if(action==='new-gift'){openForm('gift');} if(action==='edit-gift'){openForm('gift',data.id);} if(action==='delete-gift'){const name=data.name||state.gifts.find(g=>g.id===data.id)?.gift_name||'';closeModal();state.modal={kind:'confirm-delete',payload:{type:'gift',id:data.id,name}};renderModal();} if(action==='confirm-delete'){if(data.type==='product'){try{await api(`/products/${data.id}`,{method:'DELETE'});}catch{}state.products=state.products.filter(p=>p.id!==data.id);}else{try{await api(`/gifts/${data.id}`,{method:'DELETE'});}catch{}state.gifts=state.gifts.filter(g=>g.id!==data.id);}closeModal();toast(t('saved'));render();} if(action==='add-member'){openForm('member');} if(action==='remove-member'){try{await api(`/events/${state.selectedEvent.id}/members/${data.id}`,{method:'DELETE'});}catch{}state.members=state.members.filter(m=>m.id!==data.id);render();} }
  const baseActions=actions;
  actions=async function(action,data){
    if(action==='save-settings'){
      const locale=document.querySelector('#localeSelect')?.value||state.locale; const theme=document.querySelector('#themeSelect')?.value||state.theme;
      const username=document.querySelector('#usernameSetting')?.value.trim()||state.user.username; const display_name=document.querySelector('#displayNameSetting')?.value.trim()||state.user.display_name; const password=document.querySelector('#passwordSetting')?.value||'';
      if(!username||!display_name){toast(t('username')+' / '+t('displayName'));return;}
      try { const result=await api('/auth/account',{method:'PATCH',body:JSON.stringify({username,display_name,password})}); state.user=result.user; const listed=state.users.find(u=>u.id===state.user.id); if(listed)Object.assign(listed,state.user); await api('/auth/preferences',{method:'PATCH',body:JSON.stringify({locale,theme})}); }
      catch(error) { if(!state.demo){toast(error.message || '保存失败');return;} state.user={...state.user,username,display_name,locale,theme}; const u=state.users.find(x=>x.id===state.user.id);if(u)Object.assign(u,state.user); }
      state.locale=locale; state.theme=theme; localStorage.setItem('locale',locale); localStorage.setItem('theme',theme); toast(t('saved')); render(); return;
    }
    if(action==='edit-user'){const account=state.users.find(u=>u.id===data.id);if(account)openForm('user',account.id);return;}
    if(action==='edit-member'){
      const member=state.members.find(m=>m.id===data.id); if(!member)return;
      openForm('member',member.id); return;
    }
    return baseActions(action,data);
  };
  function closeModal(){state.modal=null;document.querySelector('.modal-backdrop')?.remove();}
  function decorateModal(kind,id){
    const modal=document.querySelector('.modal-backdrop #modalForm');
    if(!modal)return;
    if(kind==='user'){
      const account=state.users.find(u=>u.id===id)||{}; const title=modal.querySelector('h3'); if(title)title.textContent=id?t('editAccount'):t('account');
      const username=modal.querySelector('input[name="username"]'); const display=modal.querySelector('input[name="display_name"]'); const password=modal.querySelector('input[name="password"]');
      if(id){if(username)username.value=account.username||'';if(display)display.value=account.display_name||'';if(password){password.required=false;const label=password.parentElement.querySelector('label');if(label)label.textContent=t('newPassword');const hint=document.createElement('small');hint.style.color='var(--muted)';hint.textContent=t('passwordOptional');password.parentElement.appendChild(hint);}}
      const role=modal.querySelector('select[name="role"]'); if(role){role.innerHTML=`<option value="operator">${t('operator')}</option><option value="event_admin">event_admin</option><option value="admin">${t('adminRole')}</option>`;role.value=account.role||'operator';}
      if(id&&!modal.querySelector('select[name="status"]')){const actions=modal.querySelector('.modal-actions');const field=document.createElement('div');field.className='field';field.innerHTML=`<label>${t('status')}</label><select name="status"><option value="active">active</option><option value="disabled">disabled</option></select>`;field.querySelector('select').value=account.status||'active';modal.insertBefore(field,actions);}
      const submit=modal.querySelector('.modal-actions .btn-primary');if(submit)submit.textContent=id?t('update'):t('create');
    }
    if(kind==='member'){
      const member=state.members.find(m=>m.id===id);
      const title=modal.querySelector('h3'); if(title)title.textContent=id?t('editMember'):t('addMember');
      if(id){const userSelect=modal.querySelector('select[name="user_id"]');if(userSelect){userSelect.value=member?.id||'';userSelect.disabled=true;const hidden=document.createElement('input');hidden.type='hidden';hidden.name='user_id';hidden.value=member?.id||'';userSelect.parentElement.appendChild(hidden);}const role=modal.querySelector('select[name="event_role"]');if(role)role.value=member?.event_role||'operator';}
    }
    if(kind==='event'){
      const input=modal.querySelector('input[name="currency_unit"]');
      if(input){const wrap=input.parentElement;wrap.innerHTML=`<label>${t('currencyUnit')}</label>${currencySelect(input.value)}`;}
    }
    if(kind==='product'){
      const input=modal.querySelector('input[name="price_minor"]');
      if(input){const minorInput=document.createElement('input');minorInput.type='hidden';minorInput.name='price_minor';minorInput.value=minorAmount(input.value);input.name='price_major';input.step='0.01';input.value=majorAmount(input.value);input.parentElement.appendChild(minorInput);input.oninput=()=>{minorInput.value=minorAmount(input.value);};const label=input.parentElement.querySelector('label');if(label)label.textContent=amountLabel(t('amount'),state.selectedEvent?.currency_unit);}
    }
    if(kind==='gift'){
      const input=modal.querySelector('input[name="threshold_minor"]');
      if(input){const minorInput=document.createElement('input');minorInput.type='hidden';minorInput.name='threshold_minor';minorInput.value=minorAmount(input.value);input.name='threshold_major';input.step='0.01';input.value=majorAmount(input.value);input.parentElement.appendChild(minorInput);input.oninput=()=>{minorInput.value=minorAmount(input.value);};const label=input.parentElement.querySelector('label');if(label)label.textContent=amountLabel(t('giftThreshold'),state.selectedEvent?.currency_unit);}
    }
  }
  function openForm(kind,id){state.modal={kind,id};renderModal();decorateModal(kind,id);}
  function renderModal(){ if(!state.modal)return; const el=document.createElement('div'); el.className='modal-backdrop'; let html=''; const kind=state.modal.kind; if(kind==='confirm'){const c=calcCart();html=`<div class="modal"><h3>${t('confirmTitle')}</h3><p>${t('confirmText')}<br><strong>${t('total')}: ${money(c.net)}</strong></p><div class="modal-actions"><button class="btn btn-ghost" data-action="close-modal">${t('cancel')}</button><button class="btn btn-primary" data-action="confirm-order">${t('confirm')}</button></div></div>`;} else if(kind==='event'){const e=state.events.find(x=>x.id===state.modal.id)||{};html=`<form class="modal" id="modalForm"><h3>${state.modal.id?t('update'):t('addEvent')}</h3><div class="field"><label>${t('eventName')}</label><input name="name" required value="${esc(e.name||'')}"></div><div class="form-grid"><div class="field"><label>${t('currencyUnit')}</label><input name="currency_unit" value="${esc(e.currency_unit||'元')}"></div><div class="field"><label>${t('state')}</label><select name="manual_status"><option value="draft" ${e.manual_status==='draft'?'selected':''}>${t('draft')}</option><option value="open" ${e.manual_status==='open'?'selected':''}>${t('open')}</option><option value="scheduled" ${e.manual_status==='scheduled'?'selected':''}>${t('scheduled')}</option><option value="closed" ${e.manual_status==='closed'?'selected':''}>${t('closed')}</option></select></div><div class="field"><label>${t('startAt')}</label><input type="datetime-local" name="start_at" value="${e.start_at?new Date(e.start_at).toISOString().slice(0,16):''}"></div><div class="field"><label>${t('endAt')}</label><input type="datetime-local" name="end_at" value="${e.end_at?new Date(e.end_at).toISOString().slice(0,16):''}"></div></div><div class="modal-actions"><button type="button" class="btn btn-ghost" data-action="close-modal">${t('cancel')}</button><button class="btn btn-primary">${state.modal.id?t('update'):t('create')}</button></div></form>`;} else if(kind==='user'){html=`<form class="modal" id="modalForm"><h3>${t('account')}</h3><div class="field"><label>${t('username')}</label><input name="username" required autocomplete="off"></div><div class="field"><label>${t('displayName')}</label><input name="display_name" required></div><div class="field"><label>${t('passwordHint')}</label><input name="password" required type="password" autocomplete="new-password"></div><div class="field"><label>${t('role')}</label><select name="role"><option value="operator">${t('operator')}</option><option value="admin">${t('adminRole')}</option></select></div><div class="modal-actions"><button type="button" class="btn btn-ghost" data-action="close-modal">${t('cancel')}</button><button class="btn btn-primary">${t('create')}</button></div></form>`;} else if(kind==='member'){html=`<form class="modal" id="modalForm"><h3>${t('addMember')}</h3><div class="field"><label>${t('account')}</label><select name="user_id" required>${state.users.filter(u=>u.id!==state.user.id).map(u=>`<option value="${u.id}">${esc(u.display_name)} (@${esc(u.username)})</option>`).join('')}</select></div><div class="field"><label>${t('role')}</label><select name="event_role"><option value="operator">${t('operator')}</option><option value="event_admin">${t('adminRole')}</option></select></div><div class="modal-actions"><button type="button" class="btn btn-ghost" data-action="close-modal">${t('cancel')}</button><button class="btn btn-primary">${t('create')}</button></div></form>`;} else if(kind==='product'){const p=state.products.find(x=>x.id===state.modal.id)||{};const hasImg=!!p.image_key;html=`<form class="modal" id="modalForm"><h3>${state.modal.id?t('editProduct'):t('product')}</h3><div class="field"><label>${t('product')}</label><div class="upload-zone" id="uploadZone" data-role="upload">${hasImg?`<img class="upload-preview" src="${esc(p.image_key)}" alt=""><button type="button" class="upload-remove" data-role="remove-image">${icon('x')}</button>`:`<span class="upload-hint">${t('uploadImage')}</span>`}</div><input type="file" id="imageInput" accept="image/jpeg,image/png,image/webp,image/gif" hidden></div><div class="field"><label>${t('product')}</label><input name="name" required value="${esc(p.name||'')}"></div><div class="field"><label>${t('amount')} (分)</label><input name="price_minor" type="number" min="0" step="1" value="${p.price_minor!=null?p.price_minor:1000}" required></div><div class="modal-actions">${state.modal.id?`<button type="button" class="btn btn-danger" data-action="delete-product" data-id="${state.modal.id}" data-name="${esc(p.name||'')}" style="margin-right:auto">${t('deleteProduct')}</button>`:''}<button type="button" class="btn btn-ghost" data-action="close-modal">${t('cancel')}</button><button class="btn btn-primary">${state.modal.id?t('update'):t('create')}</button></div></form>`;} else if(kind==='gift'){const g=state.gifts.find(x=>x.id===state.modal.id)||{};html=`<form class="modal" id="modalForm"><h3>${state.modal.id?t('update'):t('addGift')}</h3><div class="field"><label>${t('giftThreshold')}</label><input name="threshold_minor" type="number" min="0" step="1" value="${g.threshold_minor!=null?g.threshold_minor:5000}" required></div><div class="field"><label>${t('giftName')}</label><input name="gift_name" required value="${esc(g.gift_name||'')}"></div><div class="field"><label>${t('mode')}</label><select name="mode"><option value="highest" ${g.mode==='highest'?'selected':''}>${t('highest')}</option><option value="cumulative" ${g.mode==='cumulative'?'selected':''}>${t('cumulative')}</option></select></div><div class="modal-actions"><button type="button" class="btn btn-ghost" data-action="close-modal">${t('cancel')}</button><button class="btn btn-primary">${state.modal.id?t('update'):t('create')}</button></div></form>`;} else if(kind==='confirm-delete'){const p=state.modal.payload||{};html=`<div class="modal"><h3>${p.type==='gift'?t('deleteGift'):t('deleteProduct')}</h3><p>${p.type==='gift'?t('deleteGiftConfirm'):t('deleteProductConfirm')}</p><div class="modal-actions"><button class="btn btn-ghost" data-action="close-modal">${t('cancel')}</button><button class="btn btn-danger" data-action="confirm-delete" data-type="${p.type}" data-id="${esc(p.id)}">${t('confirmDelete')}</button></div></div>`;} el.innerHTML=html; document.body.appendChild(el); el.querySelectorAll('[data-action]').forEach(b=>b.onclick=()=>actions(b.dataset.action,b.dataset)); if(kind==='product'){const pending={file:null,remove:false};const zone=el.querySelector('#uploadZone');const input=el.querySelector('#imageInput');const showImg=url=>{zone.innerHTML=`<img class="upload-preview" src="${esc(url)}" alt=""><button type="button" class="upload-remove" data-role="remove-image">${icon('x')}</button>`;el.querySelector('[data-role="remove-image"]').onclick=()=>{pending.file=null;pending.remove=true;zone.innerHTML=`<span class="upload-hint">${t('uploadImage')}</span>`;};};zone.onclick=()=>input.click();input.onchange=()=>{const file=input.files[0];if(!file)return;const allowed=['image/jpeg','image/png','image/webp','image/gif'];if(!allowed.includes(file.type)){toast(t('unsupportedImage'));return;}if(file.size>5*1024*1024){toast(t('imageTooLarge'));return;}pending.file=file;pending.remove=false;const reader=new FileReader();reader.onload=()=>showImg(reader.result);reader.readAsDataURL(file);};if(el.querySelector('[data-role="remove-image"]'))el.querySelector('[data-role="remove-image"]').onclick=()=>{pending.file=null;pending.remove=true;zone.innerHTML=`<span class="upload-hint">${t('uploadImage')}</span>`;};state.modal._pending=pending;} const form=el.querySelector('#modalForm'); if(form)form.onsubmit=event=>{event.preventDefault();submitForm(kind,state.modal.id,new FormData(form));}; }
  async function submitOrder(){const c=calcCart();const order={id:'o'+Date.now(),event_id:state.selectedEvent.id,order_no:`${new Date().toISOString().slice(0,10).replaceAll('-','')}-${String(state.orders.length+1).padStart(3,'0')}`,client_request_id:crypto.randomUUID(),net_amount_minor:c.net,positive_amount_minor:c.sales,return_amount_minor:c.returns,status:'confirmed',created_at:new Date().toISOString(),items:c.items.map(i=>({product_id:i.id,product_name_snapshot:i.name,unit_price_snapshot:i.price_minor,quantity:i.quantity,line_amount_minor:i.line}))};try{const result=await api(`/events/${state.selectedEvent.id}/orders`,{method:'POST',body:JSON.stringify(order)});state.orders.unshift(result.order||order);state.cart={};closeModal();toast(t('orderDone'));state.screen='orders';render();}catch(error){closeModal();toast(error.message || 'Order failed');}}
  async function cancelOrder(id){if(!confirm(t('cancelOrder')+'?'))return;try{await api(`/orders/${id}/cancel`,{method:'POST',body:JSON.stringify({reason:'operator correction'})});}catch{}const o=state.orders.find(x=>x.id===id);if(o)o.status='cancelled';localStorage.setItem('demoOrders',JSON.stringify(state.orders));render();}
  async function submitForm(kind,id,formData){ const body=Object.fromEntries(formData.entries()); if(body.start_at)body.start_at=new Date(body.start_at).toISOString(); if(body.end_at)body.end_at=new Date(body.end_at).toISOString(); if(kind==='event'){ try { if(id){const result=await api(`/events/${id}`,{method:'PATCH',body:JSON.stringify(body)}); const index=state.events.findIndex(e=>e.id===id); if(index>=0)state.events[index]=result.event;state.selectedEvent=result.event;} else {const result=await api('/events',{method:'POST',body:JSON.stringify(body)});state.events.unshift(result.event);state.selectedEvent=result.event;} } catch { if(id){Object.assign(state.events.find(e=>e.id===id),body);} else {const event={id:'e'+Date.now(),slug:body.name.toLowerCase().replace(/[^a-z0-9]+/g,'-')+'-'+Math.random().toString(36).slice(2,7),...body};state.events.unshift(event);state.selectedEvent=event;} localStorage.setItem('demoEvents',JSON.stringify(state.events)); } } if(kind==='user'){try{const result=await api('/users',{method:'POST',body:JSON.stringify(body)});state.users.unshift(result.user);}catch{state.users.unshift({id:'u'+Date.now(),...body,status:'active'});} } if(kind==='member'){const event=state.selectedEvent;try{await api(`/events/${event.id}/members`,{method:'POST',body:JSON.stringify(body)});}catch{}const u=state.users.find(x=>x.id===body.user_id);if(u&&!state.members.some(m=>m.id===u.id))state.members.push({...u,event_role:body.event_role});} if(kind==='product'){const event=state.selectedEvent||state.events[0];if(event){body.price_minor=Number(body.price_minor)||0;const pending=state.modal&&state.modal._pending;try{if(pending&&pending.file){body.image_key=(await uploadImage(pending.file,event.id)).url;}else if(pending&&pending.remove){body.image_key=null;}if(id){const result=await api(`/products/${id}`,{method:'PATCH',body:JSON.stringify(body)});Object.assign(state.products.find(p=>p.id===id),result.product);}else{const result=await api(`/events/${event.id}/products`,{method:'POST',body:JSON.stringify(body)});state.products.push(result.product);}}catch{if(id){Object.assign(state.products.find(p=>p.id===id),body);}else{state.products.push({id:'p'+Date.now(),event_id:event.id,name:body.name,price_minor:body.price_minor,image_key:body.image_key||null});}}}} if(kind==='gift'){const event=state.selectedEvent||state.events[0];if(event){body.threshold_minor=Number(body.threshold_minor)||0;try{if(id){const result=await api(`/gifts/${id}`,{method:'PATCH',body:JSON.stringify(body)});Object.assign(state.gifts.find(g=>g.id===id),result.rule);}else{const result=await api(`/events/${event.id}/gifts`,{method:'POST',body:JSON.stringify(body)});state.gifts.push(result.rule);}}catch{if(id){Object.assign(state.gifts.find(g=>g.id===id),body);}else{state.gifts.push({id:'g'+Date.now(),event_id:event.id,threshold_minor:body.threshold_minor,gift_name:body.gift_name,mode:body.mode||'highest',active:1});}}}} closeModal();toast(t('saved'));render(); }
  const baseSubmitForm=submitForm;
  submitForm=async function(kind,id,formData){
    if(kind==='user'&&id){
      const body=Object.fromEntries(formData.entries()); const payload={...body}; if(!payload.password)delete payload.password;
      try { const result=await api(`/users/${id}`,{method:'PATCH',body:JSON.stringify(payload)}); const index=state.users.findIndex(u=>u.id===id); if(index>=0)state.users[index]=result.user; if(state.user.id===id)state.user=result.user; }
      catch(error) { if(!state.demo){closeModal();toast(error.message || '保存失败');return;} const account=state.users.find(u=>u.id===id); if(account){Object.assign(account,payload);} if(state.user.id===id)state.user={...state.user,...payload}; }
      closeModal(); toast(t('saved')); render(); return;
    }
    if(kind==='member'&&id){const body=Object.fromEntries(formData.entries());const member=state.members.find(m=>m.id===id);if(member)member.event_role=body.event_role==='event_admin'?'event_admin':'operator';}
    return baseSubmitForm(kind,id,formData);
  };
  // Safari exposes gesture events separately from touch-action; cancel them as a fallback.
  const preventGestureZoom = event => event.preventDefault();
  document.addEventListener('gesturestart', preventGestureZoom, { passive: false });
  document.addEventListener('gesturechange', preventGestureZoom, { passive: false });
  document.addEventListener('gestureend', preventGestureZoom, { passive: false });
  window.addEventListener('online',()=>document.querySelector('.status')&&(document.querySelector('.status').innerHTML='<span class="status-dot" aria-hidden="true"></span>'+t('online'))); if('serviceWorker' in navigator)navigator.serviceWorker.register('/sw.js?v=22').catch(()=>{}); boot();
})();
