# 开发计划 (Development Plan)

## 项目概述

场贩订单台 —— 面向现场贩售活动工作人员的订单录入与统计 PWA，全部基于 Cloudflare 服务（Workers + D1 + R2 + KV），不额外部署云服务器。

---

## 已完成功能 (v0.1.0)

### 基础架构

- [x] Cloudflare Worker API（鉴权、活动、商品、订单、成员、图片）
- [x] D1 数据库 schema（users, sessions, events, event_members, products, gift_rules, orders, order_items, order_gifts, audit_logs）
- [x] R2 商品图片上传与服务（`/api/events/:id/images` → `/media/:key`）
- [x] KV 命名空间绑定（预留缓存层）
- [x] PWA 应用外壳缓存（Service Worker + Manifest）
- [x] SPA fallback 路由（`/events/{slug}` 直接访问不 404）

### 鉴权与权限

- [x] 登录 / 退出（PBKDF2 密码哈希，HttpOnly Cookie 会话）
- [x] 四级角色：super_admin / admin / event_admin / operator
- [x] 不开放外部注册，仅管理员内部创建账号
- [x] 活动级权限控制（event_members 决定可见性）
- [x] 个人设置页（语言、主题偏好持久化到 D1）

### 活动管理

- [x] 创建活动（弹窗表单：名称、价格单位、状态、开售/停售时间）
- [x] 编辑活动（独立编辑页面：基础信息 + 商品列表 + 可访问人员）
- [x] 活动列表卡片（状态指示器与活动名称同排，位于卡片右上角）
- [x] 销售数据入口（仅系统管理员和活动管理员可见，使用报表图标）
- [x] 活动直接访问链接（`/events/{slug}`）

### 商品管理

- [x] 添加商品（名称、价格以分为单位）
- [x] 编辑商品（活动编辑页内联编辑名称和价格）
- [x] 商品卡片横向布局（左侧正方形图片占位，右侧名称/价格/数量步进器）
- [x] 商品网格响应式适配（移动端侧栏收起与平板/桌面侧栏展开分别按卡片最小宽度计算每行数量）

### 订单录入

- [x] 商品加购（+/- 步进器，或点击数字直接编辑，数量可为负数用于退货）
- [x] 底部结算栏（圆角卡片，固定页面底部；满赠信息在上方，无满赠时不显示）
- [x] 无退货时仅显示总额；有退货时左侧显示销售/退货分项
- [x] 下单二次确认弹窗
- [x] 订单提交后计入统计，跳转订单记录页
- [x] 订单取消（软删除，状态 cancelled，不计入统计）

### 满赠规则

- [x] 按订单净额计算满赠
- [x] 可累加模式：`数量 = floor(净额 / 门槛)`
- [x] 不可累加模式：取已满足条件中的最高门槛，数量 1
- [x] 未达到满赠时不显示满赠内容
- [x] 前端计算与展示满赠数量

### 国际化与主题

- [x] 四语言支持：zh-CN / zh-TW / en / ja
- [x] 默认跟随浏览器系统语言
- [x] 浅色 / 深色 / 跟随系统主题
- [x] 默认浅色主题

### 界面交互

- [x] 侧栏导航（活动列表 / 订单 / 活动管理 / 账号管理 / 个人设置）
- [x] 移动端侧栏抽屉（☰ 按钮展开，✕ 按钮关闭）
- [x] 移动端侧栏遮罩（点击侧栏外区域收起，并阻止背景页面交互）
- [x] 顶栏滚动时固定在页面顶部
- [x] 商品列表与底部结算栏之间保留滚动间距
- [x] 移动端锁定页面缩放，保留正常滚动和点击操作
- [x] Toast 提示
- [x] 模态弹窗（创建活动 / 创建商品 / 创建用户 / 添加成员 / 下单确认）
- [x] 账号管理与活动管理分离为独立页面

---

## 待完成功能

### 高优先级

- [x] **满赠规则管理 UI**
  - 活动编辑页新增满赠规则区域
  - 支持添加 / 编辑 / 删除满赠规则
  - 设置门槛金额、赠品名称、累加模式
  - 需要 Worker API：`POST/PATCH/DELETE /api/events/:id/gifts`

- [x] **商品图片上传 UI**
  - 商品编辑表单增加图片上传字段
  - 调用已有 `POST /api/events/:id/images` 上传至 R2
  - 图片预览与删除（R2 对象删除）

- [x] **商品删除功能**
  - 活动编辑页商品列表增加删除按钮
  - 调用已有 `DELETE /api/products/:id`（软删除 active=0）

- [x] **订单满赠持久化**
  - 提交订单时将满赠快照写入 `order_gifts` 表
  - 需要 Worker API 在创建订单时同步插入 order_gifts 记录

### 中优先级

- [ ] **用户管理完善**
  - 编辑用户信息（显示名、角色）
  - 禁用 / 启用用户（status: active / disabled）
  - 重置密码
  - 需要 Worker API：`PATCH /api/users/:id`

- [ ] **活动删除功能**
  - 管理页活动卡片增加删除按钮
  - 需确认弹窗（关联数据提示）
  - 需要 Worker API：`DELETE /api/events/:id`

- [ ] **活动访问链接复制**
  - 活动卡片增加复制链接按钮
  - 使用 `navigator.clipboard.writeText`

- [ ] **销售统计增强**
  - 按时间段筛选订单
  - 商品维度销量明细（含退货数量）
  - 满赠发放统计
  - 导出 CSV

- [ ] **订单详情查看**
  - 订单记录页点击订单号查看详情
  - 显示商品明细和满赠信息

### 低优先级

- [ ] **审计日志记录**
  - 关键操作写入 `audit_logs` 表
  - 管理员可查看审计日志

- [ ] **商品排序**
  - 拖拽调整商品展示顺序（sort_order 字段）
  - 需要 Worker API 支持批量更新排序

- [ ] **多货币单位支持**
  - 使用 events 表 `currency_scale` 字段替代前端硬编码 `/100`
  - 支持日元（无小数位）等非两位小数货币

- [ ] **订单打印 / 小票**
  - 订单详情页支持打印小票
  - 适配热敏打印机宽度

- [ ] **活动克隆**
  - 基于现有活动快速创建新活动（复制商品和满赠规则）

---

## 技术债务

| 项目 | 说明 | 建议优先级 |
|------|------|-----------|
| `newEvent()` / `newProduct()` 仍在使用 `prompt()` | 这两个函数未被调用（已被 `openForm` 替代），但代码仍残留 | 已清理（v0.2.0） |
| Demo 模式 fallback 逻辑分散 | `submitForm` / `cancelOrder` 中 try-catch 的 catch 分支会写 localStorage demo 数据 | 可保留作为离线开发支持，但应统一封装 |
| `order_gifts` 表未使用 | schema 已建表但未写入数据 | 已实现（v0.2.0，POST /orders 时由服务端按 gift_rules 重算写入） |
| `audit_logs` 表未使用 | schema 已建表但无写入逻辑 | 随审计日志功能实现 |
| 前端无路由管理 | 使用 state.screen 手动切换，浏览器前进/后退不支持 | 可引入 hashchange 或 history API |
| CSS 单文件未模块化 | 所有样式在 styles.css，随功能增长维护成本上升 | 功能稳定后可拆分 |

---

## 版本规划

| 版本 | 目标 |
|------|------|
| v0.1.0 | 基础功能上线 |
| v0.2.0 | 满赠规则管理、商品图片上传、商品删除、订单满赠持久化（当前版本） |
| v0.3.0 | 用户管理完善、活动删除、销售统计增强、订单详情 |
| v0.4.0 | 审计日志、商品排序、多货币单位、订单打印 |
| v1.0.0 | 路由管理重构、CSS 模块化、完整测试覆盖 |

---

## 部署备忘

- 每次部署前同步递增三个文件中的缓存版本号：`index.html`（`app.js?v=N`）、`app.js`（`sw.js?v=N`）、`sw.js`（`CACHE` 常量）
- 当前应用缓存版本：`v17`
- 部署命令：`npm run check && npx wrangler deploy`
- 数据库迁移：`npx wrangler d1 execute field-market-orders --remote --file=schema.sql`
- 密码哈希：`node scripts/hash-password.mjs '<password>'`
