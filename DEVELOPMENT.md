# 开发计划 (Development Plan)

## 项目概述

场贩订单台 —— 面向现场贩售活动工作人员的订单录入与统计 PWA，全部基于 Cloudflare 服务（Workers + D1 + R2 + KV），不额外部署云服务器。

## 文档核对记录（2026-08-30）

- 对照 `public/app.js`、`worker.js`、`schema.sql` 检查 v0.2.0 功能，满赠管理、图片上传/删除、用户管理、多货币和订单满赠快照均已在文档中标记完成。
- 对照当前代码检查待完善项，审计日志、商品排序和订单打印已在 v0.4.0 完整实现；活动克隆仍待开发。
- 当前应用版本为 `0.4.0`，PWA 缓存版本为 `v38`。

---

## 已完成功能 (v0.1.0-v0.3.0)

### 基础架构

- [x] Cloudflare Worker API（鉴权、活动、商品、订单、成员、图片）
- [x] D1 数据库 schema（users, sessions, events, event_members, products, gift_rules, orders, order_items, order_gifts, audit_logs）
- [x] R2 商品图片上传与服务（`/api/events/:id/images` → `/media/:key`）
- [x] KV 命名空间绑定（预留缓存层）
- [x] PWA 应用外壳缓存（Service Worker + Manifest）
- [x] SPA fallback 路由（`/events/{slug}` 直接访问不 404）

### 鉴权与权限

- [x] 登录 / 退出（PBKDF2 密码哈希，HttpOnly Cookie 会话）
- [x] 四级角色：super_admin / admin / event_admin / operator
- [x] 不开放外部注册，仅管理员内部创建账号
- [x] 活动级权限控制（event_members 决定可见性）
- [x] 个人设置页（用户名、显示名称、密码、语言和主题偏好持久化到 D1）
- [x] 修改密码二次确认，前端校验两次输入完全一致
- [x] 超级管理员保护（角色和启用状态不可修改）

### 活动管理

- [x] 创建活动（弹窗表单：名称、价格单位、状态、开售/停售时间；支持 CNY/HKD/JPY/USD，选项按界面语言本地化）
- [x] 编辑活动（独立编辑页面：基础信息 + 商品列表 + 可访问人员）
- [x] 活动成员权限可在成员列表中直接修改
- [x] 活动列表卡片（状态指示器与活动名称同排，位于卡片右上角）
- [x] 销售数据入口（仅系统管理员和活动管理员可见，使用报表图标）
- [x] 活动直接访问链接（`/events/{slug}`）

### 商品管理

- [x] 添加商品（名称、价格按活动货币主单位输入，支持两位小数并转换为最小单位存储）
- [x] 编辑商品（活动编辑页内联编辑名称和价格）
- [x] 商品卡片上下分行布局（上层左上角缩略图 + 名称/价格，下层数量步进器独占整行）
- [x] 商品网格响应式适配（桌面端最小约 176px，移动端最小约 160px，按容器宽度自动调整列数）

### 订单录入

- [x] 商品加购（+/- 步进器，或点击数字直接编辑，数量可为负数用于退货）
- [x] 底部结算栏（圆角卡片，固定页面底部；满赠信息在上方，无满赠时不显示）
- [x] 无退货时仅显示总额；有退货时左侧显示销售/退货分项
- [x] 仅活动状态为销售中时允许下单（前端入口、提交校验和 Worker 权威校验）
- [x] 下单二次确认弹窗
- [x] 订单提交后计入统计，跳转订单记录页
- [x] 订单取消（软删除，状态 cancelled，不计入统计）

### 满赠规则

- [x] 按订单净额计算满赠
- [x] 可累加模式：`数量 = floor(净额 / 门槛)`
- [x] 不可累加模式：取已满足条件中的最高门槛，数量 1
- [x] 未达到满赠时不显示满赠内容
- [x] 前端计算与展示满赠数量

### 国际化与主题

- [x] 五语言支持：zh-CN / zh-TW / zh-HK（粤语） / en / ja
- [x] 默认语言偏好为跟随系统，并按浏览器语言自动解析
- [x] 设置页提供跟随系统选项，顶栏按钮可快速切换语言
- [x] 浅色 / 深色 / 跟随系统主题
- [x] 默认浅色主题
- [x] 浅色主题颜色说明与 `color-scheme: light` 防止 Android 自动反色

### 界面交互

- [x] 侧栏导航（活动列表 / 账号管理 / 个人设置）；订单记录从活动下单页右上角进入
- [x] 常驻侧栏按钮（☰）：桌面/平板从侧边收起或展开，手机从顶部覆盖展开
- [x] 移动端侧栏遮罩（点击侧栏外区域收起，并阻止背景页面交互）
- [x] 侧栏高度固定为视口高度，不随页面内容滚动高度拉伸
- [x] 活动卡片不展示地址文本；数据、编辑、复制链接按钮位于“进入”活动按钮左侧
- [x] 顶栏滚动时固定在页面顶部
- [x] 商品列表与底部结算栏之间保留滚动间距
- [x] 移动端锁定页面缩放，保留正常滚动和点击操作
- [x] Toast 提示
- [x] 模态弹窗（创建活动 / 创建商品 / 创建用户 / 编辑用户 / 添加成员 / 下单确认）
- [x] 账号管理与活动管理分离为独立页面

### v0.2.0

- [x] 满赠规则管理 UI（活动编辑页增删改规则，支持门槛、赠品名称和累加模式）
- [x] 商品图片上传、预览与删除（上传至 R2）
- [x] 商品删除（软删除 `active=0`）
- [x] 订单满赠持久化（服务端按规则重算并写入 `order_gifts`）
- [x] 用户管理完善（编辑、启用/禁用、重置密码及个人账号设置）
- [x] 多货币单位支持（CNY/HKD/JPY/USD，金额按最小单位存储）

### v0.3.0

- [x] 活动删除（仅系统管理员/管理员可操作；从编辑页永久清理关联数据）
- [x] 活动访问链接复制（活动卡片使用 `navigator.clipboard.writeText`）
- [x] 销售统计增强（日期筛选、商品销量/退货、满赠统计和 CSV 导出）
- [x] 订单详情查看（订单记录页查看商品明细和满赠快照）

### v0.4.0

- [x] 审计日志记录与管理员查看
- [x] 商品拖拽排序及批量保存
- [x] 订单详情打印小票（热敏纸宽度样式）

---

## 待完成功能

### 低优先级

- [ ] **活动克隆**
  - 基于现有活动快速创建新活动（复制商品和满赠规则）

---

## 技术债务

| 项目 | 说明 | 建议优先级 |
|------|------|-----------|
| `newEvent()` / `newProduct()` 仍在使用 `prompt()` | 这两个函数未被调用（已被 `openForm` 替代），但代码仍残留 | 已清理（v0.2.0） |
| Demo 模式 fallback 逻辑分散 | 已限制为 localhost Demo 模式，并通过统一检查与持久化入口隔离生产请求失败 | 已完成（v0.4.0） |
| `order_gifts` 表未使用 | schema 已建表但未写入数据 | 已实现（v0.2.0，POST /orders 时由服务端按 gift_rules 重算写入） |
| `audit_logs` 表未使用 | 已由 v0.4.0 审计日志接口写入并提供管理员查询 | 已完成 |
| 前端无路由管理 | 已使用 History API 同步活动、编辑、报表、订单、账号、审计和设置页面 | 已完成（v0.4.0） |
| CSS 单文件未模块化 | 所有样式在 styles.css，随功能增长维护成本上升 | 功能稳定后可拆分 |
| `currency_scale` 尚未贯通前端 | schema 支持自定义最小单位，但前端金额换算仍固定按 100；JPY 等零位小数货币暂不适用 | 中 |
| 自动化测试覆盖不足 | 已增加路由与 Demo 隔离测试；业务渲染和 Worker API 仍缺少集成测试 | 中 |

---

## 版本规划

| 版本 | 目标 |
|------|------|
| v0.1.0 | 基础功能上线 |
| v0.2.0 | 满赠规则管理、商品图片上传、商品删除、订单满赠持久化、多货币单位 |
| v0.3.0 | 活动删除、销售统计增强、订单详情 |
| v0.4.0 | 审计日志、商品排序、订单打印（当前版本） |
| v1.0.0 | `currency_scale` 贯通、CSS 模块化、完整测试覆盖 |

---

## 部署备忘

- 每次部署前同步递增缓存版本号：`index.html`（静态资源 `?v=N`）、`app.js`（`sw.js?v=N`）、`sw.js`（`CACHE` 常量及 `SHELL` 资源）
- 当前应用缓存版本：`v38`
- antd 图标资源生成：`npm run icons:generate`
- 首次安装依赖或升级图标库后先运行：`npm install`
- 部署命令：`npm run check && npx wrangler deploy`
- 数据库迁移：`npx wrangler d1 execute field-market-orders --remote --file=schema.sql`
- 密码哈希：`node scripts/hash-password.mjs '<password>'`
